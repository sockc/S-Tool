pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // 添加 Xposed 官方仓库，解决 api:82 找不到的问题
        maven { url "https://api.xposed.info/" }
    }
}

rootProject.name = "UnicomHook"
include ':app'
